---
- name: Apply baseline configuration to all nodes
  hosts: cluster
  become: true
  roles:
    - baseline

- name: Install and configure Docker on all nodes
  hosts: cluster
  become: true
  roles: 
    - docker
